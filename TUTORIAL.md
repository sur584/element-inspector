# 元素检查器 - 使用教程

## 目录

1. [安装插件](#安装插件)
2. [基本使用](#基本使用)
3. [快捷键说明](#快捷键说明)
4. [复制格式说明](#复制格式说明)
5. [设置页面说明](#设置页面说明)
6. [常见问题](#常见问题)

---

## 安装插件

### Windows / Mac 通用步骤

#### 第一步：构建插件

打开终端（Windows 用 PowerShell 或 CMD，Mac 用 Terminal），进入项目目录：

```bash
cd element-inspector
pnpm install
pnpm build:chrome
```

构建成功后，插件文件在 `.output/chrome-mv3/` 目录中。

#### 第二步：加载到 Chrome / Edge 浏览器

**Chrome 浏览器：**

1. 打开 Chrome，地址栏输入 `chrome://extensions/` 回车
2. 右上角打开「开发者模式」开关
3. 点击「加载已解压的扩展程序」按钮
4. 选择 `.output/chrome-mv3` 文件夹
5. 插件安装完成，工具栏会出现插件图标

**Edge 浏览器：**

1. 打开 Edge，地址栏输入 `edge://extensions/` 回车
2. 左侧打开「开发人员模式」开关
3. 点击「加载解压缩」按钮
4. 选择 `.output/chrome-mv3` 文件夹
5. 插件安装完成

#### 第三步：加载到 Firefox 浏览器

```bash
pnpm build:firefox
```

1. 打开 Firefox，地址栏输入 `about:debugging#/runtime/this-firefox` 回车
2. 点击「临时载入附加组件」按钮
3. 选择 `.output/firefox/manifest.json` 文件
4. 插件安装完成

### Mac 用户特别说明

- Mac 上的 **Command** 键在插件中对应 **Ctrl** 键。例如快捷键 `Ctrl+Shift+I` 在 Mac 上按 `Command+Shift+I`
- 如果使用 Safari 浏览器，需要先在 Safari 设置中开启「开发」菜单，然后通过「开发」→「允许未签名的扩展」来加载插件

---

## 基本使用

### 启用检查器

1. 点击浏览器工具栏中的插件图标（放大镜图标）
2. 在弹出的面板中，打开「元素检查器」开关
3. 检查器已开启，状态显示「检查器已开启」

### 查看元素信息

1. 将鼠标悬停在页面上的任意元素上
2. 元素周围会出现蓝色高亮边框
3. 同时显示一个信息悬浮窗，包含：
   - 标签名（蓝色显示，如 `<div>`）
   - CSS 选择器路径
   - 类名、ID、文本内容
   - 计算样式、内联样式
   - 尺寸位置、子元素、父元素等

### 复制元素信息

**方法一：点击复制按钮**

在信息悬浮窗底部，点击「复制」按钮即可复制当前元素信息。

**方法二：使用快捷键**

悬停在元素上时，按 `C` 键直接复制。

**方法三：切换复制格式**

点击悬浮窗右下角的格式选择器，可以选择不同的复制格式（AI友好格式、JSON、HTML片段、CSS选择器）。

### 粘贴给 AI

复制完成后，打开任意 AI 聊天工具（ChatGPT、Claude、豆包等），直接 `Ctrl+V`（Mac 用 `Command+V`）粘贴即可。AI 会收到结构化的元素信息，可以帮你修改样式或代码。

---

## 快捷键说明

### Windows 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+Shift+I` | 开启/关闭检查器 |
| `C` | 复制当前悬停元素的信息 |
| `X` | 冻结/解冻当前元素信息 |
| `Escape` | 关闭检查器 |

### Mac 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Command+Shift+I` | 开启/关闭检查器 |
| `C` | 复制当前悬停元素的信息 |
| `X` | 冻结/解冻当前元素信息 |
| `Escape` | 关闭检查器 |

> **注意：** `C` 和 `X` 快捷键仅在鼠标悬停在元素上且焦点不在输入框中时生效。

### 冻结模式

按 `X` 键可以冻结当前显示的元素信息，这样你可以：
- 移动鼠标查看其他元素，但悬浮窗内容不变
- 方便对比不同元素的信息
- 再次按 `X` 解冻，恢复正常悬停检测

---

## 复制格式说明

插件支持 4 种复制格式，适合不同的使用场景：

### 1. AI 友好格式（默认）

结构化的文本格式，方便直接粘贴给 AI 进行修改。包含标签、选择器、类名、样式、尺寸等信息，末尾带有修改提示模板。

示例输出：
```
## 元素信息
**标签:** <div>
**选择器:** #app > main > section.hero > div.card
**类名:** card card--featured
**ID:** featured-card
**内联样式:** padding: 20px; border-radius: 8px;
**计算样式:** display: flex, width: 300px, color: #333333
**尺寸:** 300px x 200px
**父元素:** <section class="hero">
**子元素 (3):** h2, p, button
---
请帮我修改这个元素。我想要：
1. [描述你的修改]
```

### 2. JSON 格式

包含所有信息的结构化 JSON 数据，适合程序处理或批量分析。

### 3. HTML 片段

HTML 代码片段，适合粘贴到 CodePen、JSFiddle 等在线编辑器中测试。

### 4. CSS 选择器

CSS 选择器路径和相关样式规则，适合在样式表中快速定位元素。

---

## 设置页面说明

点击 Popup 底部的「打开完整设置」链接，进入详细设置页面。

### 信息字段配置

可以开启或关闭每个信息字段的显示：

| 分类 | 字段 | 说明 |
|------|------|------|
| 基础信息 | 标签名、ID、类名、类列表 | 元素的基本标识信息 |
| 内容信息 | 文本内容、内部HTML、外部HTML | 元素的内容 |
| 样式信息 | 计算样式、内联样式、伪元素 | 元素的样式信息 |
| 结构信息 | 属性、数据属性、子元素、父元素、CSS选择器 | DOM 结构信息 |
| 布局信息 | 尺寸位置 | 元素的尺寸和位置 |

### 覆盖层外观

- **位置**: 工具提示跟随光标、固定在元素顶部、或显示在底部栏
- **主题**: 亮色、暗色、跟随系统自动切换
- **最大宽度**: 工具提示的最大宽度（默认 400px）
- **高亮颜色**: 悬停时元素边框的高亮颜色（默认蓝色 #2196F3）

### 高级设置

- **排除选择器**: 不需要检查的元素选择器列表（如 script、style 等）
- **Shadow DOM**: 是否包含 Shadow DOM 内部的元素
- **节流率**: 鼠标悬停事件的节流间隔（默认 50ms）
- **最大文本长度**: 文本内容和 HTML 的截断长度（默认 200 字符）

### 导入/导出

可以将完整配置导出为 JSON 文件，方便在不同设备间同步设置。

---

## 常见问题

### Q: 插件安装后看不到图标？

- 检查是否点击了「开发者模式」开关
- 尝试刷新扩展管理页面
- 确认选择的是正确的文件夹（`.output/chrome-mv3`）

### Q: 悬停时看不到信息悬浮窗？

- 确认插件开关已开启
- 检查是否按了 `X` 键进入了冻结模式
- 尝试刷新页面后重新开启检查器

### Q: 按 C 键没有复制？

- 确认鼠标正在悬停在元素上
- 检查焦点是否在输入框中（输入框中快捷键不生效）
- 尝试使用鼠标点击悬浮窗中的「复制」按钮

### Q: Mac 上快捷键不工作？

- Mac 用户需要将 `Ctrl` 替换为 `Command` 键
- 例如 `Ctrl+Shift+I` → `Command+Shift+I`
- `C` 和 `X` 键不需要修改，直接按即可

### Q: 悬浮窗显示位置不对？

- 悬浮窗会自动避开浏览器边界
- 如果元素靠近页面边缘，悬浮窗会自动调整位置
- 可以在设置中调整「最大宽度」

### Q: 如何恢复默认设置？

- 在设置页面底部点击「恢复默认设置」按钮
- 或者在扩展管理页面删除扩展后重新安装

### Q: 支持哪些浏览器？

- Chrome 88+
- Edge 88+
- Firefox 85+
- 其他基于 Chromium 的浏览器（如 Brave、Vivaldi）

### Q: 插件安全吗？

- 插件不会收集任何个人信息
- 所有数据仅存储在本地浏览器中
- 插件代码完全开源，可自行审查

---



---

*最后更新：2026年6月4日*
